apply plugin: "war"

bootWar {
    mainClassName = "software.jevera.demo.openid.blog.BlogApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
